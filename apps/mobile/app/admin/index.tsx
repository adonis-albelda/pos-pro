import { AdminWebView } from "@/components/admin-webview";

/** Web admin dashboard — replaces the native tile launcher at /admin. */
export default function AdminHome() {
  return <AdminWebView />;
}
